<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG-BUILD ONLY (lives under src/debug/, never merged into the release
  build). Permits cleartext HTTP to the emulator's host-loopback alias only,
  so a local reverse proxy on the host machine can stand in for the real
  Supabase backend when the emulator's own network can't reach the internet
  directly. Do not widen this or copy it into src/main/.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
